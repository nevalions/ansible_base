---
- name: Remove Longhorn Folders and Data from Workers
  hosts: workers_all
  become: true
  gather_facts: false
  serial: "30%"
  vars_files:
    - vault_secrets.yml
  tags:
    - longhorn
    - storage
    - cleanup
    - remove
  roles:
    - longhorn_clean
