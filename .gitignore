*.logs
*.log
*.ini
env_*

# Ansible logs (contains sensitive connection data and IPs)
ansible.log

# Environment configuration files
env_vars.json

# Ignore group_vars/*.yml but keep example files
group_vars/*.yml

# Keep example files
!group_vars/*.example.yml

# Ignore ansible-vault encrypted files
*.vault
group_vars/*.vault.yml
vault_secrets.yml

# Ignore inventory files (contain real hostnames and IPs)
hosts_*.ini
inventory.ini
inventory/*.ini

# Ignore vault password file
.vault_pass

# Ignore GPG-encrypted vault password and client script
vault_password.gpg
vault_password_client.sh

# Ignore WireGuard key files (contains private keys)
wg_keys.yaml
*_wg_keys.yaml

# Ignore WireGuard configuration files (may contain endpoints and IPs)
wg*.conf

 # Ignore implementation guides (contains network details)
 WIREGUARD_IMPLEMENTATION.md
 WIREGUARD_SETUP.md

# SSH keys for Ansible connections
ansible_id_*
!ansible_id_*.pub.example

# Backup files (may contain sensitive data)
*.bak
*.backup
*~

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Ansible lint cache
.ansible-lint
.ansible-lint*

# Ansible retry files
*.retry
