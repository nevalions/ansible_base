---
- name: Soft reset Kubernetes Worker Node (preserve packages)
  hosts: workers_all
  become: true
  gather_facts: false
  serial: 1
  vars_files:
    - vault_secrets.yml
  tags:
    - kubernetes
    - k8s
    - reset
    - cleanup
    - worker
    - soft_reset
  vars:
    remove_container_images: false
    remove_kubernetes_packages: false
  roles:
    - kuber_reset
