---
- name: Install and manage Helm on Kubernetes control-plane nodes
  hosts: kuber_small_planes
  become: true
  gather_facts: true
  serial: 1
  tags:
    - kubernetes
    - k8s
    - helm
    - addon
  vars_files:
    - vault_secrets.yml
  roles:
    - helm
