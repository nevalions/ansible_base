---
- name: Verify Kubernetes Cluster Health
  hosts: bay_plane1
  become: true
  gather_facts: true
  serial: 1
  vars_files:
    - vault_secrets.yml
  tags:
    - kubernetes
    - k8s
    - verify
    - test
  roles:
    - kuber_verify
