---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ cert_manager_clusterissuer_staging_name }}
spec:
  acme:
    email: "{{ cert_manager_acme_email }}"
    server: "{{ cert_manager_acme_server_staging }}"
    privateKeySecretRef:
      name: {{ cert_manager_clusterissuer_staging_private_key_secret_name }}
    solvers:
      - http01:
          ingress:
            class: {{ cert_manager_http01_ingress_class }}
